 //need to get the data from the tumbler blog
 //likes = the user liking the posts and blogs
 //hashtags = tags in the blog the user liked??
 //
var likes = new Array();
var hashtags =  new Array();

for(var i = 0; i < likes.length; i++)
{
    localStorage.setItem["likes"] = JSON.stringify.push(likes[i]);
}
for(var j=0; j < hashtags.length; j++)
{
    localStorage.setItem["hashtags"] = JSON.stringify.push(hashtags[j]);
}

/*for(var i = 0; i < localStorage.length; i++)
 {
    likes[i] = 
    hashtags[i] = 
        localStorage["likes"] = JSON.stringify(likes[]);
        localStorage["hashtags"] = JSON.stringify(hashtags[]);
        document.write(likes[i]);
        document.write(hashtags[i]);
    } */
   

   /*
<!--<p><textarea id="persisted-text" rows="5" cols="50"></textarea><br>
    <script>
    function localstorage()
            {
                localStorage["names"] = JSON.stringify('names');
                if (window.localStorage) {
                var p = document.querySelector('#persisted-text');
                if (localStorage.text == null) {
                localStorage.text = p.value = supported;
                } else {
        p.value = localStorage.text;
        }
    p.addEventListener('keyup', function(){ localStorage.text = p.value; }, false);
    } else {
        document.getElementById('persisted-text').value = unsupported;
        }
    }
    </script></p>      -->             */

s
